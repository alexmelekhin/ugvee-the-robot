# ugvee-the-robot

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`alexmelekhin/ugvee-the-robot`), managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each mapped to a label of the same name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
